<br>
<p style="color: green"><%= notice %></p>
<div class="row">
<%= render @item %>
  <div class="col-md-4">
  <br>
      <%= link_to "Back", items_path, class: "btn btn-dark btn-rectangular" %> |
      <%= link_to "Edit", edit_item_path(@item), class: "btn btn-outline-dark btn-rectangular" %> 
  </div>
  <div class="col-md-8 d-flex justify-content-end">
      <%= button_to "Delete", @item, method: :delete, class: "btn btn-rectangular btn-outline-danger btn-sm" %>
  </div>
</div>