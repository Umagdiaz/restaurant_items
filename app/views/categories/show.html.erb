<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%= link_to "Edit", edit_category_path(@category), class: "btn btn-outline-dark btn-rectangular"  %> |
  <%= link_to "Back to categories", categories_path, class: "btn btn-dark btn-rectangular"  %>
  <br>
  <br>
  <%= button_to "Delete", @category, method: :delete, class: "btn btn-rectangular btn-outline-danger" %>
</div>
