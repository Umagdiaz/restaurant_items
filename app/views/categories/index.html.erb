<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show", category, class: "btn btn-outline-dark btn-rectangular btn-sm" %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path, class: "new-link" %>
